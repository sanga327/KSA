
// 예제 4-1. 산술 연산자와 대입 연산자

#include <stdio.h>
int main(void)
{
	int a,b;
	int sum, sub, mul, inv;

	a = 10;
	b = 20;
 	sum = a+b;   // 덧셈
	sub = a-b;   // 뺄셈
	mul = a*b;   // 곱셈
	inv = -a;   //음수 연산

	return 0;
}